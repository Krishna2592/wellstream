name: Docker — Build & Push Images

on:
  push:
    branches: [ "main", "master" ]
  release:
    types: [ published ]

env:
  REGISTRY: ghcr.io
  IMAGE_PRODUCER: ghcr.io/${{ github.repository_owner }}/wellstream-producer
  IMAGE_SPARK:    ghcr.io/${{ github.repository_owner }}/wellstream-spark-processor
  IMAGE_AI:       ghcr.io/${{ github.repository_owner }}/wellstream-ai-agent

jobs:
  build-jars:
    name: Compile & Package JARs
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Configure Maven settings (Confluent + Spring repos)
        run: |
          mkdir -p ~/.m2
          cat > ~/.m2/settings.xml <<'EOF'
          <settings>
            <profiles>
              <profile>
                <id>custom-repos</id>
                <repositories>
                  <repository>
                    <id>confluent</id>
                    <url>https://packages.confluent.io/maven/</url>
                  </repository>
                  <repository>
                    <id>spring-releases</id>
                    <url>https://repo.spring.io/release</url>
                    <snapshots><enabled>false</enabled></snapshots>
                  </repository>
                  <repository>
                    <id>spring-milestones</id>
                    <url>https://repo.spring.io/milestone</url>
                    <snapshots><enabled>false</enabled></snapshots>
                  </repository>
                </repositories>
              </profile>
            </profiles>
            <activeProfiles>
              <activeProfile>custom-repos</activeProfile>
            </activeProfiles>
          </settings>
          EOF

      - name: Build all JARs
        run: mvn --batch-mode clean package -DskipTests

      - name: Upload producer JAR
        uses: actions/upload-artifact@v4
        with:
          name: producer-jar
          path: producer/target/producer-1.0.0-jar-with-dependencies.jar

      - name: Upload spark-processor JAR
        uses: actions/upload-artifact@v4
        with:
          name: spark-processor-jar
          path: spark-processor/target/spark-processor-jar-with-dependencies.jar

      - name: Upload ai-agent JAR
        uses: actions/upload-artifact@v4
        with:
          name: ai-agent-jar
          path: ai-agent/target/ai-agent-1.0.0.jar

  push-images:
    name: Build & Push Docker Images
    runs-on: ubuntu-latest
    needs: build-jars
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Download producer JAR
        uses: actions/download-artifact@v4
        with:
          name: producer-jar
          path: producer/target/

      - name: Download spark-processor JAR
        uses: actions/download-artifact@v4
        with:
          name: spark-processor-jar
          path: spark-processor/target/

      - name: Download ai-agent JAR
        uses: actions/download-artifact@v4
        with:
          name: ai-agent-jar
          path: ai-agent/target/

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract metadata — Producer
        id: meta-producer
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_PRODUCER }}
          tags: |
            type=sha,format=short
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master' }}
            type=semver,pattern={{version}},enable=${{ github.event_name == 'release' }}

      - name: Extract metadata — Spark Processor
        id: meta-spark
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_SPARK }}
          tags: |
            type=sha,format=short
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master' }}
            type=semver,pattern={{version}},enable=${{ github.event_name == 'release' }}

      - name: Extract metadata — AI Agent
        id: meta-ai
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_AI }}
          tags: |
            type=sha,format=short
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master' }}
            type=semver,pattern={{version}},enable=${{ github.event_name == 'release' }}

      - name: Build & push — wellstream-producer
        uses: docker/build-push-action@v5
        with:
          context: producer
          push: true
          tags: ${{ steps.meta-producer.outputs.tags }}
          labels: ${{ steps.meta-producer.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build & push — wellstream-spark-processor
        uses: docker/build-push-action@v5
        with:
          context: spark-processor
          push: true
          tags: ${{ steps.meta-spark.outputs.tags }}
          labels: ${{ steps.meta-spark.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build & push — wellstream-ai-agent
        uses: docker/build-push-action@v5
        with:
          context: ai-agent
          push: true
          tags: ${{ steps.meta-ai.outputs.tags }}
          labels: ${{ steps.meta-ai.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
